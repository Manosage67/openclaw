[phases.setup]
nixPkgs = ["nodejs_22", "pnpm", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = [
  "echo 'Skipping UI build on Railway free plan'"
]

[start]
cmd = "python -m openclaw.app"
